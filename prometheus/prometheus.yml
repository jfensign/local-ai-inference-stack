global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'llama-swap'
    static_configs:
      - targets: ['llama-swap:8080'] # Scrapes llama-swap container internally

  - job_name: 'dcgm-exporter'
    static_configs:
      - targets: ['dcgm-exporter:9400'] # Scrapes GPU VRAM, power, and temps
